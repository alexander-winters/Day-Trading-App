const User = require('../models/user');

async function create_user() {
    try {

    } catch (error) {
        // Handle the error
        console.error(error);
        throw new Error('Error creating user');
    }
}